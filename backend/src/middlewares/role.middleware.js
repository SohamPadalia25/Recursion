// Shim to keep the correct filename used by routes.
// The actual implementation currently lives in `role.midddleware.js` (typo in filename).
export { isInstructor, isAdmin } from "./role.midddleware.js";

